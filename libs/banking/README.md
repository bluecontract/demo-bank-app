# banking

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build banking` to build the library.

## Running unit tests

Run `nx test banking` to execute the unit tests via [Vitest](https://vitest.dev/).
