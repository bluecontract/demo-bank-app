# Bug register

Use this file to track blockers discovered while building the PayNote suite.
Production bank logic must not be changed in this track to make tests pass; the
correct response is to document the blocker with evidence and root cause.

## Current status

At least one local live scenario still depends on additional harness/event-flow
alignment before it can be enabled reliably.

## Entries

| ID      | Scenario                                               | Layer                    | Severity | Status | Symptom                                                                                                                                                                                  | Root cause                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Evidence                                                                                                                                                                                                                                                                                                              | Workaround                                                                                                                                             |
| ------- | ------------------------------------------------------ | ------------------------ | -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BUG-001 | `card-delivery-capture`                                | local live integration   | medium   | open   | The local harness can identify and accept a delivery, but the follow-up root PayNote session never materializes into a saved PayNote record, so no capture is produced after acceptance. | The bank delivery acceptance path explicitly defers PayNote bootstrap to later webhook processing. In the local harness, replaying a delivery `DOCUMENT_EPOCH_ADVANCED` event with `Conversation/Document Bootstrap Requested` can trigger parts of the continuation flow, but the currently simulated sequence still does not produce a persisted root PayNote record for the expected session. This points to an incomplete local MyOS harness/document-event simulation rather than a proven bank-ledger defect. | Runtime evidence from targeted runs: raw delivery records become visible and identified; `acceptPayNote` runs successfully; replayed bootstrap continuation events still leave `getRawPayNoteBySessionId(...)` as `null`; account activity remains stuck at the original `HOLD_CREATED` item with no capture/posting. | Keep the scenario implemented but skipped for now; extend the local harness/document fixtures and continuation-event simulation before re-enabling it. |
| BUG-002 | `pending-install-approval-capture`                     | local live integration   | medium   | open   | The pending-action scenario is not yet runnable end to end in the local harness.                                                                                                         | The suite still lacks a deterministic local continuation that produces the contract pending action visible to the bank read model after delivery/bootstrap steps.                                                                                                                                                                                                                                                                                                                                                   | Current local helper coverage reaches delivery identification and transfer-based captures, but not the pending-action continuation required by this scenario.                                                                                                                                                         | Keep the scenario implemented as skipped until the local harness/read-model continuation is aligned.                                                   |
| BUG-003 | `milestones` / `subscription` / `voucher` serial flows | local serial integration | medium   | open   | Complex serial flows are not yet runnable with the current local harness coverage.                                                                                                       | These flows depend on richer MyOS continuation chains (milestones, mandate bootstrap, voucher monitoring, cashback) that have not yet been modeled end to end in the local harness.                                                                                                                                                                                                                                                                                                                                 | Active fast scenarios pass, but the richer continuation events required by these families are still absent from the runnable suite.                                                                                                                                                                                   | Keep the serial scenarios implemented as skipped until the local harness and fixtures cover the required event chains.                                 |
